- content_for :title do
  Beemind.me - Home

.container#main
  .row
    - if current_user
      = render "my_goals"
    - else
      = render "welcome"
  .row
    = render "metrics"
  - unless current_user
    .row
      %p
        To start,
        =link_to "sign in", signin_path
        with your Beeminder account.

